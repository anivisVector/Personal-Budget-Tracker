# Example environment file (DO NOT put real secrets here)
# Copy to .env for local development.

# Flask session secret (change for production)
SECRET_KEY=change_this_in_production

# Optional: where to store the SQLite file
# SQLITE_PATH=budget_tracker.db

# Optional: enable debug locally (1 = on, 0 = off)
# FLASK_DEBUG=1
